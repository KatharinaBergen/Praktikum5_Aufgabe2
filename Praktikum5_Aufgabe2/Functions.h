/*
Author: Kat Bergen

header to declare functions 

PAD1 P5A2
*/

#ifndef TITLE
#define TITLE

int countElements(const char*); //counts elements in array pointer points to 
void print(const char*); //prints elements in array pointer points to 
void tolower(char*); //converts all char elements in array to lower 
char* cstring_copier(const char*); //copies elements in array pointer points to to another array another pointer points to
void check();

#endif // !TITLE
